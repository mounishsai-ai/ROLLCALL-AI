fastapi==0.110.0
uvicorn==0.29.0
python-multipart==0.0.9
firebase-admin>=6.5.0
deepface==0.0.93
# 4.9 is compiled against the NumPy 1.x ABI. With NumPy 2 installed it fails
# at import with "numpy.core.multiarray failed to import" — which is exactly
# what broke the first Cloud Run build, while the laptop was fine on 4.13.
# Keep this at or above 4.10 so the container matches the machine it was
# developed on.
opencv-python>=4.10
tf-keras
numpy>=2.0
faiss-cpu
pydantic==2.6.4
python-dotenv==1.0.1
# AdaFace dependencies
torch
torchvision
pytorch_lightning
huggingface_hub

# VLM dependencies.
# google-genai is the current SDK and talks to both Vertex AI (Google Cloud
# project, real quota) and AI Studio (API key). google-generativeai is the
# retired predecessor and is no longer imported by this project.
google-genai
